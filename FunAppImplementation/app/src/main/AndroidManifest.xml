<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="raivona.pmf.funapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".kviz.QuizActivity"></activity>
        <activity android:name=".kviz.data.DbHelper" />
        <activity android:name=".anagrami.AnagramFinish" />
        <activity android:name=".anagrami.AnagramGameActivity" />
        <activity android:name=".anagrami.AnagramActivity" />
        <activity android:name=".iksoks.IksOksActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>